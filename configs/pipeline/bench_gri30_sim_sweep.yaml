# Benchmark: Cantera multi-condition sweep (gri30)
steps:
  - id: sim
    task: sim.sweep_csv
    inputs: {}
    params:
      mechanism_path: ${mechanism.path}
      conditions_file: ${benchmarks.conditions_file}
      model: gri30
  - id: viz
    task: viz.ds_dashboard
    inputs: { runs: "@sim" }
    params: { dashboard: ds }
